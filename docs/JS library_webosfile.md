A file on the webos's file system.

Since [1.0beta1](../releases/tag/1.0beta1).

# Methods

* **readAsBinary**(_Webos.Callback_ **callback**) : Get this file's content, encoded in base64.
 * _Webos.Callback_ **callback** The callback.
* **writeAsBinary**(_String_ **contents**, _Webos.Callback_ **callback**) : Write this file's content, encoded in base64.
 * _String_ **contents** The base64-encoded content.
 * _Webos.Callback_ **callback** The callback.

# Static methods

